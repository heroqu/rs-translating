/**
 * A sample.
 * Here we assume that the main site is located
 * inside the sibling directory '../site/'
 *
 * The production version should not be tracked
 * as it depends on actual sub-projects location
 * in each particular machine.
 *
 * Configure paths to main React app
 */
module.exports = {
  src: '../site/src/components',
  dest: '../site/src/i18n'
}
